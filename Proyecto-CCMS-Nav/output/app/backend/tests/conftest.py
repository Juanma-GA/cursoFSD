# Fixtures compartidas por toda la suite: engine/sesión SQLite en memoria (StaticPool), Base.metadata.create_all() importando todos los backend/models/*.py, y monkeypatch de SessionLocal en cada uno de los 13 módulos de storage/ (adaptación del monkeypatch único de memory_store.SessionLocal en fase-5-herramientas-vibe-coder/api/test_topics.py a la estructura de storage/ por agregado de este proyecto). También define las fixtures de JWT/usuario/proyecto reutilizadas por integration/ y security/.
